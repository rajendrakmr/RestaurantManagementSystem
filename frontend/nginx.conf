server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Redirect root to /apps/login
    location = / {
        return 302 /apps/login;
    }

    # React SPA handling
    location / {
        try_files $uri $uri/ /index.html;
    }
}
